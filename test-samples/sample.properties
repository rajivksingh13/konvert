# Application Configuration
app.name=Konvert
app.version=1.0.0
app.description=Universal Format Converter
app.author=Your Name

# Database Configuration
database.host=localhost
database.port=5432
database.name=mydb
database.username=admin
database.password=secret123
database.ssl.enabled=true
database.ssl.verify=true

# Server Configuration
server.host=0.0.0.0
server.port=8080
server.timeout=30000
server.maxConnections=100

# Feature Flags
features.darkMode.enabled=true
features.export.enabled=true
features.batchProcessing.enabled=false

# Logging
logging.level=INFO
logging.file.path=/var/log/konvert
logging.file.maxSize=10MB
logging.file.maxFiles=5

